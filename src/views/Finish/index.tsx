import { Typography } from '@material-ui/core';
import React from 'react';

const Finish: React.FC = () => {
  return (
    <div>
      <Typography variant="h3">Pedido enviado para o restaurante!</Typography>
    </div>
  );
};

export default Finish;
